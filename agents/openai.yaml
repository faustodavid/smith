interface:
  display_name: "Smith"
  short_description: "ADO search/grep + DevOps read workflows"
  default_prompt: "Use $smith to run Thanos-style Azure DevOps search, grep, PR, build, and board read workflows with local credentials."
